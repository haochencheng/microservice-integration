server:
  port: 4002
  servlet:
    context-path: /app
spring:
  application:
    name: microservice-integration-app
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: dev
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
